name: Schedule

on:
    schedule:
     - cron: '5 * * * *'

jobs:
    testSchedule:
        runs-on: ubuntu-latest

        steps:
            - name: Run on schedule
              run: echo "This test runs at 16h25 🕒"